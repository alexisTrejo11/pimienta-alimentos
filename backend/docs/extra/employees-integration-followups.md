# Employees module — integration test follow-ups

## Notes from employee manager OpenAPI / multipart update (2026-05-14)

- Added `update_multipart_employeeJsonPart_returns200` to lock `PUT /api/v1/employees/{id}` with `multipart/form-data` + JSON `employee` part (same binding pattern as register).
- **No extra follow-ups from this pass.**
