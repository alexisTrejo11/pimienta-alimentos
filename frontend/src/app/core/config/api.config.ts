/**
 * Base path for the Spring Boot API (see {@code @RequestMapping} on controllers).
 * Use a relative URL so the same build can be served behind a reverse proxy that forwards `/api`.
 */
export const API_BASE_URL = '/api/v1';
